# Google Gemini API Key
# 1. 访问 https://aistudio.google.com/app/apikey 获取密钥
# 2. 将密钥填入下方（本地开发请创建 .env 文件并将此内容复制进去）
# 3. 注意：在 Vite 项目中，必须以 VITE_ 为前缀，变量名必须是 VITE_GEMINI_API_KEY
# 4. 如果部署在 Vercel，请在 Vercel 控制面板的 Environment Variables 中添加同样的变量。

VITE_GEMINI_API_KEY=your_api_key_here
